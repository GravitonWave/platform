ingress:
  enabled: true
  className: nginx
  className:
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: gitea.local
      paths:
        - path: /
          pathType: Prefix