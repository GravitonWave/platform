ingress:
  enabled: true
  apiVersion: networking.k8s.io/v1
  className: nginx
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: gitea.local
      paths:
        - path: /
          pathType: Prefix