kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: bootstrap
# 2 control plane nodes and 2 workers
nodes:
  # the control plane nodes
  - role: control-plane
  - role: control-plane
  # the two workers
  - role: worker
  - role: worker
